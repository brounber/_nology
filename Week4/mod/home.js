const rectanglePerim = (height,width) => height *2 + width * 2;
const rectangleArea = (height,width) => height * width;

const circlePerim = (radius) => 2 * Math.Pi * radius;
const circleArea = (radius) => Math.Pi * radius **2;

