package org.example;

//3.  Create an abstract class (Player) that will let you create a new player for any sport
//
//    > It should have two attributes: a name and a number
//    > It should have setters and getter for both attributes
//    > It should have an abstract class (getSport)

public class Player {
    T name;
    T number;
}
