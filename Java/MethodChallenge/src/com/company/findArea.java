package com.company;

public class findArea {
    public double radius;

    public findArea(double radius) {
        this.radius = radius;
    }

    public double calculateArea (radius) {
        double calculateArea = Math.PI*radius*radius;

        return System.out.printf("Area of Circle is: %d", calculateArea() );
    }




}
